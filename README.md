# android-wireless-mirroring-fedora
One-command Fedora setup for ADB + Scrcpy wireless Android screen mirroring. Installs dependencies, prepares Scrcpy, connects via USB first, then launches wireless mirroring with custom bitrate and resolution.
